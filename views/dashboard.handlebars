<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>
  <div id="dashboard">
    <div class="container">
      <div class="row">
        <div class="col-12">
          <h1>Dashboard</h1>
          <p>Welcome! Please see below for the latest posts and events happening in your neighbourhood.</p>
          <h2>Posts</h2>
          <div class="container blog">
            <div class="row">
              <!--Waiting for sign-up/login code
    <div class="col-md-2 col-sm-2">
      <a class="btn btn-default" href="/authors">Manage Authors</a>
    </div>
    -->
              <div class="col-md-2 col-sm-2">
                <!--New post modal button-->
                <button id="postBtn">New Post</button>
              </div>
            </div><!--end row-->
            <!--New post modal form-->
            <div class="modal" id="postModal">
              <div class="modal-content col-lg-4">
                <form id="cms" name="cms">
                  <div class="form-group">
                    <label for="title">Title:</label> <input class="form-control" id="post-title" placeholder="Post Title" type="text"><br>
                    <label for="body">Body:</label> 
                    <textarea class="form-control" id="post-body" placeholder="Post Body" rows="10"></textarea> <!--Waiting for sign-up/profile code
          <div class="form-group">
            <label for="category">Select Author:</label>
            <select class="form-control" id="author">
            </select>
          </div>-->
                    <br>
                    <button class="btn btn-success submit" id="submit-post" type="submit">Submit</button> <button class="btn btn-info" id="postClose">Close</button>
                  </div>
                </form>
              </div>
            </div>
          </div><!-- Posts -->
          <ul class="list-group" id="post-list">
            <li style="list-style: none">{{#each Posts}}</li>
            <li class="list-group-item" data-id="{{this.id}}">
              <p>{{this.postTitle}}</p>
              <p>{{this.postBody}}</p><button class="btn btn-danger float-right delete">ｘ</button>
            </li>
            <li style="list-style: none">{{/each}}</li>
          </ul>
          <hr>
          <h2>Events</h2>
          <div id="map"></div>
          <div class="row">
            <div class="col-lg-3 offset-3">
              <button id="addEventBtn">Add an Event</button> <!-- The Modal -->
              <div id="addEventModal">
                <!-- Modal content -->
                <div class="modal-content col-lg-3">
                  <span id="addEventClose">&times;</span>
                  <form action="POST" class="clearfix mb-4">
                    <div class="form-group">
                      <label for="event-name">New Event</label> <input aria-describedby="event-name" class="form-control" id="event-name" placeholder="Enter New Event" type="text">
                    </div>
                    <div class="form-group">
                      <label for="event-type">Event Type</label> <input aria-describedby="event-type" class="form-control" id="event-type" placeholder="Enter Event Type">
                    </div>
                    <div class="form-group">
                      <label for="event-description">Event Description</label> 
                      <textarea aria-describedby="event-description" class="form-control" id="event-description" placeholder="Enter Event Description"></textarea>
                    </div>
                    <div class="form-group">
                      <label for="event-location">Event Location</label> <input aria-describedby="event-location" class="form-control" id="event-location" placeholder="Enter Event Location">
                    </div><button class="btn btn-primary float-right" id="submit-event">Submit</button>
                  </form>
                </div>
              </div>
            </div>
            <div class="col-lg-3">
              <!-- Trigger/Open The Modal -->
              <button id="allEventsBtn">View All Current Events</button> <!-- The Modal -->
              <div class="modal" id="allEventsModal">
                <!-- Modal content -->
                <div class="modal-content col-lg-4">
                  <span id="allEventsClose">&times;</span>
                  <ul class="list-group" id="events-list">
                    <li style="list-style: none">{{#each Events}}</li>
                    <li class="list-group-item" data-id="{{this.id}}">
                      <p>{{this.eventName}}</p>
                      <p>{{this.eventType}}</p>
                      <p>{{this.eventDescription}}</p>
                      <p>{{this.eventLocation}}</p><button class="btn btn-danger float-right delete">ｘ</button>
                    </li>
                    <li style="list-style: none">{{/each}}</li>
                  </ul>
                </div>
              </div>
            </div>
            <button id="submit-event" class="btn btn-primary float-right" >Submit</button>
          </form>
        </div>
      </div>
    </div>
    <div class="col-lg-3">
      <!-- Trigger/Open The Modal -->
      <button id="allEventsBtn">View All Current Events</button>
      <!-- The Modal -->
        <div id="allEventsModal" class="modal">
        <!-- Modal content -->
          <div class="modal-content col-lg-4">
          <span id="allEventsClose">&times;</span>
            <ul id="events-list" class="list-group">
              {{#each Events}}
              <li data-id="{{this.id}}" class="list-group-item">
                <p>{{this.eventName}}</p>
                <p>{{this.eventType}}</p>
                <p>{{this.eventDescription}}</p>
                <p>{{this.eventLocation}}</p>
                <button class="btn btn-danger float-right delete">ｘ</button>
              </li>
              {{/each}}
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>


<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="/js/index.js"></script>
<script src="js/blog.js" type="text/javascript"></script>
<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyC83gYW2lRHk0-0EYyDuaC-NXsDWarTXEI&libraries=places&callback=initialize"></script>


</body>
</html>
